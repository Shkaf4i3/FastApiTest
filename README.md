# FastAPI Crud Template

A simple FastAPI application with CRUD interactions with users.

## Local Run
1) Create a new PostgreSQL db
2) Create a new .env file (using .env.example template)
3) Launch the terminal and register `fastapi dev .\src\main.py`
